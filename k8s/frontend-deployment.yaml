# Generated by GitHub Copilot
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-frontend
  namespace: workflow-agents-prod
  labels:
    app.kubernetes.io/name: workflow-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: workflow-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: workflow-frontend
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: frontend
          image: autonomous-workflow-optimization-agents-frontend
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

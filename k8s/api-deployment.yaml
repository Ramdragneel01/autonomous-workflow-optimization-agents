# Generated by GitHub Copilot
apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-api
  namespace: workflow-agents-prod
  labels:
    app.kubernetes.io/name: workflow-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: workflow-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: workflow-api
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api
          image: autonomous-workflow-optimization-agents-api
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
              name: http
          envFrom:
            - configMapRef:
                name: workflow-api-config
            - secretRef:
                name: workflow-api-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
